---
spring:
  config.activate.on-profile: cloud
cds:
  datasource:
    auto-config.enabled: true
    binding: "taskmanager-hana"
  security:
    mock:
      users:
        - name: taskmanager
          password: admin123
  messaging:
    services:
      name: "taskmanager-events"
      kind: "enterprise-messaging"
      queue: 
        name: "user-registration"
